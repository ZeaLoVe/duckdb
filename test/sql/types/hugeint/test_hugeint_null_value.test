# name: test/sql/types/BIGINT/test_BIGINT_null_value.test
# description: Test BIGINT null value handling
# group: [BIGINT]



query I
SELECT NULL::BIGINT
----
NULL

statement ok
CREATE TABLE BIGINTs(id INTEGER, h BIGINT)

statement ok
INSERT INTO BIGINTs VALUES (1, NULL), (1, 1), (1, 2)

query III
SELECT id, FIRST(h), LAST(h) FROM BIGINTs WHERE h IS NULL GROUP BY id
----
1	NULL	NULL

query II
SELECT h, SUM(id) FROM BIGINTs GROUP BY h ORDER BY 1
----
NULL	1
1	1
2	1

query III
SELECT id, h1.h, h2.h FROM BIGINTs h1 JOIN BIGINTs h2 USING (id) WHERE h1.h IS NULL AND h2.h IS NULL
----
1	NULL	NULL

query I
SELECT (SELECT h1.h) FROM BIGINTs h1 ORDER BY 1
----
NULL
1
2

query I
SELECT h1.h NOT IN (SELECT h1.h+h2.h FROM BIGINTs h2) FROM BIGINTs h1 ORDER BY 1
----
NULL
NULL
NULL
